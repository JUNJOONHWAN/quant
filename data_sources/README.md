# Data Sources

Data-source contracts for quant workflows live here. Keep source-specific
access rules, failure patterns, canonical URLs/pages, extraction fields, and QA
checks in the matching subfolder.

The analysis owner is the `quant` project. Trading/runtime execution belongs to
`STOCK`.
